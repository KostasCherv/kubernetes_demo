apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: k8s-microservices
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api-gateway
  ports:
  - port: 3000
    targetPort: 3000
